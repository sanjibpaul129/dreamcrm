<%= form_for(@plc) do |f| %>
  <% if @plc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@plc.errors.count, "error") %> prohibited this plc from being saved:</h2>

      <ul>
      <% @plc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="col-lg-5 col-lg-offset-3">
    <div class="well bs-component">
      <legend>&nbspGenerate Plc</legend>
      <div class="row">
        <div class="form-group">    
          <%= f.label :'Name', :class => 'col-lg-3 control-label' %>
          <div class="col-lg-8">
            <%= f.text_field :name, :class => 'form-control'%>
          </div>
        </div>
      </div>
      <div class="form-group row">
        <div class="col-lg-6 col-lg-offset-4">
          <br>
          <%= f.submit :class => 'btn btn-primary btn-block btn_width'%>
        </div>
      </div> 
<% end %>
