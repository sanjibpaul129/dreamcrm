<%= form_for(@marketing_number) do |f| %>
  <% if @marketing_number.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@marketing_number.errors.count, "error") %> prohibited this marketing_number from being saved:</h2>

      <ul>
      <% @marketing_number.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %><br>
    <%= f.text_field :number %>
  </div>
  <div class="field">
    <%= f.label :organisation_id %><br>
    <%= f.number_field :organisation_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
