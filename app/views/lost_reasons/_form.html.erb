<%= form_for(@lost_reason) do |f| %>
  <% if @lost_reason.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lost_reason.errors.count, "error") %> prohibited this lost_reason from being saved:</h2>

      <ul>
      <% @lost_reason.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="col-lg-5 col-lg-offset-3">
    <div class="well bs-component">
      <legend>&nbspGenerate Lost Reason</legend>

      <div class="row">
        <div class="form-group">    
          <%= f.label :'Description', :class => 'col-lg-3 control-label' %>
          <div class="col-lg-8">
            <%= f.text_field :description, :class => 'form-control'%>
          </div>
        </div>
      </div>
      <div class="form-group row">
        <div class="col-lg-6 col-lg-offset-4">
          <br>
          <%= f.submit :class => 'btn btn-primary btn-block btn_width'%>
        </div>
      </div>
    </div>
  </div>

  
  <!-- <div class="actions">
    <%= f.submit %>
  </div> -->
<% end %>
