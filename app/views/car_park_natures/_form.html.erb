<%= form_for(@car_park_nature) do |f| %>
  <% if @car_park_nature.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@car_park_nature.errors.count, "error") %> prohibited this car_park_nature from being saved:</h2>

      <ul>
      <% @car_park_nature.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="col-lg-5 col-lg-offset-3">
    <div class="well bs-component">
      <legend>&nbspGenerate Car Park Nature</legend>
      <div class="row">
        <div class="form-group">
          <%= f.label :wheels, :class => 'col-lg-3 control-label' %>
          <div class="col-lg-8">
            <%= f.number_field :wheels, :class => 'form-control'%>
          </div>
        </div>
      </div><br>
      <div class="row">
        <div class="form-group">    
          <%= f.label :'Description', :class => 'col-lg-3 control-label' %>
          <div class="col-lg-8">
            <%= f.text_field :description, :class => 'form-control'%>
          </div>
        </div>
      </div>
      <div class="form-group row">
        <div class="col-lg-6 col-lg-offset-4">
          <br>
          <%= f.submit :class => 'btn btn-primary btn-block btn_width'%>
        </div>
      </div>
<% end %>
